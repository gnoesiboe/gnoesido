export type ActiveSortedTodosReducerState = Array<number>;

export function activeSortedTodosReducer(currentState: ActiveSortedTodosReducerState = [], action: Action) : ActiveSortedTodosReducerState {
    return currentState;
}
